[phases.build]
cmds = [
  "false",
  "echo 'DEBUG: Build phase running!'",
  "ls -l",
  "npm install -g typescript",
  "npm ci",
  "ls -l src",
  "tsc",
  "ls -l dist"
]

[phases.start]
cmds = ["node dist/app.js"]
